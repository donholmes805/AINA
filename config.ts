// This file is no longer used and can be deleted.
// API keys are managed securely via environment variables.

// For local development, create a .env file in the project root with your key:
// API_KEY=AIzaSy...your...key

// For production deployment (e.g., on Vercel), set the API_KEY in the 
// project's "Environment Variables" settings. The application is already
// configured to use it automatically.